#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main() {
	int a, b;
	int c;
	scanf("%d %d", &a, &b);
	scanf("%d", &c);

	b += c;

	while (b >= 60) {
		b -= 60;
		a++;
	}

	if (a > 23) {
		a -= 24;
	}

	printf("%d %d", a, b);
	return 0;
}
